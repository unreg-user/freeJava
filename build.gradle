plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = 'wta.freeJava'
version = '1.0'

repositories {
    mavenCentral()
}

gradlePlugin {
    plugins {
        freeJava {
            id = 'wta.freeJava'
            implementationClass = 'wta.freeJava.Main'
        }
    }
}